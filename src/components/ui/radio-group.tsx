import React from 'react';export function RadioGroup({ value, onValueChange, children }: any){return <div onChange={(e:any)=>onValueChange?.(e.target.value)}>{React.Children.map(children,(c:any)=> React.cloneElement(c,{groupValue:value}))}</div>;}export function RadioGroupItem({ value, groupValue }: any){return <input type='radio' value={value} name='rg' defaultChecked={groupValue===value} />;}
